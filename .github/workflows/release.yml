name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-mod:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build mod
        working-directory: mod
        run: ./gradlew build

      - name: Get version from tag
        id: version
        run: |
          if [ "${{ github.ref_type }}" = "tag" ]; then
            echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          else
            echo "version=dev" >> $GITHUB_OUTPUT
          fi

      - name: Prepare artifact
        run: |
          mkdir -p release-files
          cp mod/build/libs/mccli-*.jar release-files/
          # Remove sources and javadoc jars if present
          rm -f release-files/*-sources.jar release-files/*-javadoc.jar

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mccli-mod
          path: release-files/*.jar

  release:
    needs: [build-mod]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Flatten artifacts
        run: |
          mkdir -p release-files
          find artifacts -name "*.jar" -exec cp {} release-files/ \;
          ls -la release-files/

      - name: Generate checksums
        working-directory: release-files
        run: |
          sha256sum * > SHA256SUMS.txt
          cat SHA256SUMS.txt

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          name: MC-CLI ${{ github.ref_name }}
          body: |
            ## MC-CLI Fabric Mod

            Download the JAR and place it in your Minecraft mods folder.

            **Requirements:**
            - Minecraft 1.21.11
            - Fabric Loader 0.18.4+
            - [Fabric API](https://modrinth.com/mod/fabric-api)

            ## Installation

            1. Download `mccli-1.0.0.jar` below
            2. Copy to your mods folder:
               - **Linux**: `~/.minecraft/mods/`
               - **macOS**: `~/Library/Application Support/minecraft/mods/`
               - **Windows**: `%APPDATA%\.minecraft\mods\`
            3. Install the Python CLI: `pip install git+https://github.com/Th0rgal/mc-cli.git#subdirectory=cli`

            ## Checksums

            See `SHA256SUMS.txt` for file integrity verification.
          files: release-files/*
